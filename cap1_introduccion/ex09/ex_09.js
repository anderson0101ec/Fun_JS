/* ARRAYS O ARREGLOS

un ARRAY es una estructura de datos que se utiliza para almacenar una coleccion ordenada de elemenetos .
estos elementos pueden ser cadenas de textos, valores numericpos como: enteros, decimales, valores booleanos , entre otros. pueden ser un conjunto de funciones e inclusive otros array

*/

let numeros = [];// declaracion de un array vacio

numeros = [15, 80, 650, 50.43, -10]; // se guarda el array una coleccion numerica de tipo decimal entero
console.log(numeros); // se imprime por consola todo el array
console.log(numeros[3]);//se imprime por console  el 4to elemento por que pertenece al indice 3

